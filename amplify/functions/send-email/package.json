{
  "name": "send-email",
  "version": "1.0.0",
  "description": "Lambda function to send emails using SendGrid",
  "main": "handler.js",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@sendgrid/mail": "^7.7.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.19",
    "typescript": "^5.3.3"
  }
} 